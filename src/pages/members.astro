---
import { getEntry } from "astro:content";
import Layout from "../layouts/Layout.astro";
import { useTranslations } from "../libs/i18n";
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";

const { t } = useTranslations(Astro.url);

const members = await getEntry("members", "members");
---

<Layout
    title={t({
        ja: "メンバー紹介",
        en: "Our Members",
        zh: "成员介绍",
    })}
    description={t({
        ja: "OUS Metaverseのメンバーをご紹介します。代表、副代表をはじめ、多様なスキルを持ったメンバーが揃っています。",
        en: "Introducing the members of OUS Metaverse. We have members with diverse skills, including our president and vice president.",
        zh: "介绍OUS Metaverse的成员。我们有各种技能的成员，包括代表和副代表。",
    })}
>
    <main>
        {/* Hero Section */}
        <div class="hero bg-gradient-to-b from-base-200 to-base-100 py-20">
            <div class="hero-content text-center">
                <div>
                    <h1 class="text-4xl font-bold mb-6 md:text-5xl">
                        {
                            t({
                                ja: "メンバー紹介",
                                en: "Our Members",
                                zh: "成员介绍",
                            })
                        }
                    </h1>
                    <p class="max-w-3xl text-lg">
                        {
                            t({
                                ja: "OUS Metaverseは多様なスキルと興味を持つメンバーで構成されています。一人ひとりの得意分野を活かしながら、メタバースの新たな可能性を追求しています。",
                                en: "OUS Metaverse consists of members with diverse skills and interests. While leveraging each member's strengths, we pursue new possibilities in the metaverse.",
                                zh: "OUS Metaverse由具有不同技能和兴趣的成员组成。我们在发挥每个成员的优势的同时，探索元宇宙的新可能性。",
                            })
                        }
                    </p>
                </div>
            </div>
        </div>

        {/* 運営メンバー */}
        <div class="py-16 container mx-auto px-4">
            <h2 class="text-3xl font-bold text-center mb-12">
                {
                    t({
                        ja: "幹部メンバー",
                        en: "Staff Members",
                        zh: "运营成员",
                    })
                }
            </h2>

            <div class="grid gap-8 md:grid-cols-2">
                {
                    members?.data.filter((m) => m.executive).map((member) => (
                        <div class="card lg:card-side bg-base-100 shadow-xl">
                            <figure class="lg:w-1/3 aspect-[5/3] lg:aspect-[3/4] overflow-hidden">
                                <Image
                                    src={member.avatar}
                                    alt={member.name}
                                    class="min-h-full min-w-full object-cover"
                                />
                            </figure>
                            <div class="card-body lg:w-2/3 p-6">
                                <div class="mb-2 flex justify-between items-center">
                                    <span class="badge badge-primary badge-outline">
                                        {t(member.role!)}
                                    </span>
                                </div>
                                <h3 class="card-title text-xl">{member.name}</h3>
                                <p class="text-sm opacity-70">{t(member.grade)}</p>

                                <p>{t(member.comment)}</p>

                                {member.skills && member.skills.length > 0 && (
                                    <div class="mb-4">
                                        <h4 class="text-sm font-semibold mb-2">
                                            {t({
                                                ja: "スキル・得意分野",
                                                en: "Skills & Expertise",
                                                zh: "技能和专长",
                                            })}
                                        </h4>
                                        <div class="flex flex-wrap gap-1">
                                            {member.skills.map((skill) => (
                                                <span class="badge badge-ghost">{skill}</span>
                                            ))}
                                        </div>
                                    </div>
                                )}

                                {member.socials && member.socials.length > 0 && (
                                    <div class="card-actions">
                                        {member.socials.map((social) => (
                                            <a
                                                href={social.url}
                                                target="_blank"
                                                rel="noopener noreferrer"
                                                class="btn btn-circle btn-sm btn-ghost"
                                                aria-label={social.name}
                                            >
                                                <Icon name={social.icon} class="h-4 w-4" />
                                            </a>
                                        ))}
                                    </div>
                                )}
                            </div>
                        </div>
                    ))
                }
            </div>
        </div>

        {/* Regular Members */}
        <div class="py-16 bg-base-200">
            <div class="container mx-auto px-4">
                <h2 class="text-3xl font-bold text-center mb-12">
                    {
                        t({
                            ja: "メンバー",
                            en: "Regular Members",
                            zh: "普通成员",
                        })
                    }
                </h2>

                <div class="grid gap-6 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
                    {
                        members?.data.filter((m => !m.executive)).map((member) => (
                            <div class="card bg-base-100 shadow-md transition hover:shadow-lg">
                                <div class="card-body items-center text-center">
                                    <div class="avatar mb-4">
                                        <div class="w-24 rounded-full ring ring-primary ring-offset-base-100 ring-offset-2">
                                            <Image
                                                src={member.avatar}
                                                alt={member.name}
                                                class="w-full h-full object-cover"
                                            />
                                        </div>
                                    </div>
                                    <h3 class="card-title text-lg">{member.name}</h3>
                                    <p class="text-sm opacity-70">{t(member.grade)}</p>

                                    <p class="text-sm">{t(member.comment)}</p>

                                    {member.skills && member.skills.length > 0 && (
                                        <div class="flex flex-wrap justify-center gap-1 mt-2">
                                            {member.skills.map((skill) => (
                                                <span class="badge badge-ghost">{skill}</span>
                                            ))}
                                        </div>
                                    )}

                                    {member.socials && member.socials.length > 0 && (
                                        <div class="card-actions mt-4">
                                            {member.socials.map((social) => (
                                                <a
                                                    href={social.url}
                                                    target="_blank"
                                                    rel="noopener noreferrer"
                                                    class="btn btn-circle btn-sm btn-ghost"
                                                    aria-label={social.name}
                                                >
                                                    <Icon name={social.icon} class="h-4 w-4" />
                                                </a>
                                            ))}
                                        </div>
                                    )}
                                </div>
                            </div>
                        ))
                    }
                </div>
            </div>
        </div>

        {/* Join Us Section */}
        <div class="py-16">
            <div class="container mx-auto px-4 text-center">
                <h2 class="text-3xl font-bold mb-6">
                    {
                        t({
                            ja: "私たちと一緒に活動しませんか？",
                            en: "Would You Like to Join Us?",
                            zh: "想与我们一起活动吗？",
                        })
                    }
                </h2>
                <p class="max-w-2xl mx-auto mb-8 text-lg">
                    {
                        t({
                            ja: "OUS Metaverseでは、メタバースに興味のあるメンバーを随時募集しています。プログラミング、デザイン、企画、広報など、あなたの得意分野を活かして一緒に活動しましょう！",
                            en: "OUS Metaverse is always recruiting members interested in the metaverse. Let's work together using your strengths in programming, design, planning, PR, and more!",
                            zh: "OUS Metaverse一直在招募对元宇宙感兴趣的成员。让我们一起利用您在编程、设计、规划、公关等方面的优势一起工作吧！",
                        })
                    }
                </p>
                <a href="/join" class="btn btn-primary">
                    {
                        t({
                            ja: "参加方法を見る",
                            en: "See How to Join",
                            zh: "查看如何加入",
                        })
                    }
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        class="h-5 w-5 ml-2"
                        viewBox="0 0 20 20"
                        fill="currentColor"
                    >
                        <path
                            fill-rule="evenodd"
                            d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z"
                            clip-rule="evenodd"
                        ></path>
                    </svg>
                </a>
            </div>
        </div>
    </main>
</Layout>
